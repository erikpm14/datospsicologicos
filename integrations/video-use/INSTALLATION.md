# Instalación

No hay instalación externa.

`video-use` ya vive dentro del proyecto y usa las dependencias Node del backend.

## Requisitos

- `npm install` en `backend/`
- FFmpeg disponible vía dependencias del backend

## Prueba rápida

```bash
cd backend
npm run render:video-use:test
```
